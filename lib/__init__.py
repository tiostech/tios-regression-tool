"""Shared helpers for the Trader Custom Tools Streamlit app.

This package deliberately lives outside ``pages/`` -- Streamlit renders every
``.py`` file in ``pages/`` as its own page in the sidebar, so shared modules
placed there would show up as empty pages.
"""
